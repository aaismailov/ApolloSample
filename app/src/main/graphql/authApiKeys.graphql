query AuthApiKeysQuery {
    authentication {
        apiKeys {
            id
            name
            keyShort
            expiration
            createdAt
            updatedAt
            isRevoked
        }
    }
}